# 2. Functions in Python (Preparation for next Week)


# What is function?
    # --> A set of instructions which can be used over and over if required.


# Why are functions useful?
    # --> Once a function written it can be used multiple times as requirement.
    # --> functions breaks down a large and complex problems into smaller parts.
    # --> functions can be maintained easily as we can modify the problems in single file
    

# Difference between built-in and user-defined functions.
    
    # --> built- in functions are those functions which are pre loaded into the python itself. It can be used immediately without importing and creating them manually.       
    # --> user-defined functions are. those functions which are created by the programmer for their benifits / easiness.


# Research the def keyword, parameters, arguments, and return values.
    # --> def keyword is used to define a function
    # --> parameters --> it is the placeholders for the inputs inside the parenthesis
    # --> arguments --> it is the actual value of the function that we pass and call it
    # --> return values --> it is the output result that the function sends back to the main program

# Find five built-in Python functions and explain their purpose.
    # --> type() : it returns the type of dataType of an object. It is used to determine the type of dataType
    # --> sum() : it returns the total sum of all the elements from a list or any other kind of data.
    # --> print() : it is used to print the outputs in a console screen
    # --> len() : is used to find the length of a string, list, tuple, etc
    # --> range() : it is often used in loops for  generating a sequence of numbers.